# Changelog

All notable changes to `github-workflows` are documented in this file.

## v1.0.0 - 2026-07-20

Initial release: shared reusable release workflows — `release.yml` (single-phase) plus the PR-gated
`release-prepare.yml` and `release-publish.yml`. Every action pinned by commit SHA.
